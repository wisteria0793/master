# タスクリスト: プロトタイプ開発

- [x] **Step 1: データの統合と準備**
  - [x] POIデータの所在（`.csv`等）と、景観クラスター（エンベディング結果またはクラスタID結果）の所在を確認する。
  - [x] データを結合し、1つのDataFrameとして読み込める関数を記述したスクリプト（`src/urban_analysis/prototype/data_loader.py`）を作成する。
- [x] **Step 2: 推薦ロジックの実装**
  - [x] `src/urban_analysis/prototype/recommender.py` を作成する。
  - [x] ユーザー入力のPOIクラスタと同一クラスタのものを抽出するフィルタを実装する。
  - [x] 最寄り距離（例: 2km圏内）に基づく絞り込みフィルタを実装する。
  - [x] (オプション) コサイン類似度を用いて上位3～5件を取得する処理を追加する。
- [x] **Step 3: ルート探索機能の実装**
  - [x] `src/urban_analysis/prototype/router.py` を作成する。
  - [x] `osmnx` を用いて、対象エリアの道路グラフを取得・保存する処理を実装する。
  - [x] Selected(推薦済み) POI間を結ぶ最短経路リストを計算する。
- [x] **Step 4: 可視化・UI構築**
  - [x] `src/urban_analysis/prototype/visualize.py` もしくは全体を統括する `scripts/run_prototype.py` を作成する。
  - [x] `folium` を使って、ルートとピン（基準・推薦）を描画し、`html` ファイルを出力する機能を作成する。
  - [x] プロトタイプ全体を通しでテスト実行し、ルートが意図通りに生成されるか確認する。
- [x] **Step 5: Network KDEの統合と経路最適化**
  - [x] 対象エリア（函館全域など）の歩行者用道路ネットワークを `.graphml` として事前に保存・読み込む機構を実装する。
  - [x] `router.py` に、特定のクラスタ座標群を元に部分グラフ上で Network KDE 密度を計算するロジックを組み込む。
  - [x] 単純な `length` ではなく `length / (1 + α * density)` をコスト関数としたダイクストラ探索を実装する。
  - [x] `run_prototype.py` を更新し、KDEが効いたルートになっているか（従来の最短距離から変化するか）を確認する。
- [ ] **システムの安全性・互換性のメンテナンス**
  - [x] 既存の主要ファイル（GNN学習、データ分析等）に影響が出ていないことを再点検。
  - [x] OSMnx のバージョンによるサブグラフ抽出エラーの修正。
