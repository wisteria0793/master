# 営業時間考慮・ルート生成最適化（TSPTW）タスクリスト

- [x] `utils/time_parser.py` (仮) の作成または既存ファイルへの処理追加：`output_with_google_places_jp.json` から `hours` を抽出し、正規表現を用いて `[open_time_minutes, close_time_minutes]`（0時からの経過分/秒）にパースする処理を実装。
- [x] `phase3_recommender.py` の修正：POIデータを読み込む際、JSONからパースした営業時間データを結合し、各施設に時間枠（Time Window）を付与する。
- [x] `router.py` の `_solve_tsp_held_karp` を TSPTW（Time-Window TSP）に拡張：
  - 徒歩の平均速度（約1.2m/s ≒ 72m/min）を定義し、距離コスト行列を移動時間行列に変換。
  - 各POIの滞在時間（例: 一律30分）を設定。
  - DPの値を「最小コスト」から「最小到着時刻」に変更。開店前なら待機、閉店後なら無効（枝刈り）のロジックを追加。
  - 景観コスト（NKDEコスト）は、時間とは独立したペナルティまたは移動時間への係数として組み込むか、評価関数を微調整する。
- [x] `run_phase3.py` の修正：
  - 出発時刻（例: `10:00`）を引数で受け取るようにする。
  - ルートの出力結果に「各POIへの到着予定時刻」や「総所要時間」などを表示するようにして、効率化の結果を可視化する。
