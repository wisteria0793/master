# 実験16: 函館市内限定 POI テキストクラスタリングの再実施計画

実験16における「意味と空間の分離の妥当性評価」を、市外の POI を除外した「函館市内限定」のデータセットで再計算し、より正確な都市特性を抽出します。

## ユーザーレビューが必要な事項

> [!IMPORTANT]
> - **対象 POI の変更**: 約 797 件から 702 件へと対象が絞り込まれます。これに伴い、以前算出された最適なクラスタ数 ($k=12$) やシルエットスコアの値が変化します。
> - **成果物の保存**: 以前の実験結果を上書きするか、または `hakodate_` プレフィックスを付けて別名保存するか。今回は比較のため、別名保存（または README の更新）を想定しています。

## 提案される変更

### 1. 最適化スクリプトの修正と実行
[optimize_poi_text_clusters.py](file:///Users/atsuyakatougi/Desktop/master/src/data_analysis/optimize_poi_text_clusters.py) を修正し、函館市内限定のデータでシルエットスコアを再計算します。

- **フィルタリング**: `google_places_data` の住所情報に「函館市」が含まれるもののみを抽出。
- **実行**: 新しい `hakodate_poi_text_silhouette_scores.png` を生成。

### 2. クラスタ結果の保存
新しい最適な $k$ に基づき、各 POI にクラスタ ID を付与した CSV を生成します。
- 保存先: `data/processed/poi/hakodate_poi_text_clusters.csv`

### 3. クラスタマップの生成
[map_poi_text_clusters.py](file:///Users/atsuyakatougi/Desktop/master/src/data_analysis/map_poi_text_clusters.py) を修正（または新規作成）し、函館限定版のクラスタマップを作成します。
- 出力: `docs/results/hakodate_poi_text_cluster_map.html`

### 4. README.md の更新
実験16のセクションを、函館市内限定の結果に基づいて更新します。特に「物理的距離のシルエットスコアがマイナスである」という考察が、市内限定でも維持されるか（あるいは強化されるか）を確認します。

## オープンな質問

> [!NOTE]
> - 今回、最適な $k$ を再度シルエットスコアに基づき決定しますが、以前の $k=12$ と異なる値になった場合、新しい値を採用して進めてよろしいでしょうか。

## 検証計画

### 自動テスト / 実行検証
- フィルタリング後の POI 数が 702 件であることを確認。
- ハバーサイン距離のシルエットスコアが、市外排除によってどのように変化したかを確認。

### 手動検証
- 生成されたマップを開き、市外の施設（北斗市など）がプロットされていないことを目視で確認。
